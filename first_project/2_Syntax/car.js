// const carName = 'BMW';
// let maxCarSpeed = 250;
// let carOwner = 'Олег';
//
// alert(carName);
// alert(maxCarSpeed);
// alert(carOwner);
//
// maxCarSpeed = 240;
// carOwner = 'София';
// alert(maxCarSpeed);
// alert(carOwner);
// console.log('У машины изменилась максимальная скорость', maxCarSpeed);
// console.log('У машины изменился владелец', carOwner);
//
// maxCarSpeed = 230;
// carOwner = 'Виктор';
// alert(maxCarSpeed);
// alert(carOwner);
// console.log('У машины изменилась максимальная скорость', maxCarSpeed);
// console.log('У машины изменился владелец', carOwner);
//
// maxCarSpeed = 220;
// carOwner = 'Александр';
// alert(maxCarSpeed);
// alert(carOwner);
// console.log('У машины изменилась максимальная скорость', maxCarSpeed);
// console.log('У машины изменился владелец', carOwner);


let summ = 0;
const a = [1, 3, 4, 6, 7, 8];
console.log(a);
console.log('Первый элемент массива а: ', a[0]);
console.log('последний элемент массива а: ', a[a.length - 1]);
// summ = a[0];
// console.log('', summ)

for (let i = 0; i <= a.length; i++) {
    console.log('Значение i: ', i);
    summ += i;
    console.log('Значение переменной summ: ', summ);
    }
getSumOfSequence
a[0]
a[a.length - 1]
summ = a[0] + a[a.length - 1]
prompt('введите число')
//console.log('введите число') - все выводи в консоль лог при возникновении вопросов, каждое действие с переменной, каждую переменную.
//65 - длинна массива
// 1[0] + 65[64] = 66 - первый элемент[индекс элемента]  + последний элемент[индекс последнего элемента]
//176 - длинна массива
// 1[0] + 176[175] = 177- первый элемент[индекс элемента]  + последний элемент[индекс последнего элемента]




















